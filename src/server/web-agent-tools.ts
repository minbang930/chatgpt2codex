import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import type { ToolContext } from "../types.js";

export function registerWebAgentTools(_server: McpServer, _ctx: ToolContext): void {
  // Web-worker tool registrations are added incrementally in M2.3.
}
