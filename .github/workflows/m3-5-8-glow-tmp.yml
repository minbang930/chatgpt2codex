name: M3.5.8 glow polish temporary

on:
  push:
    branches: [dev/custom-runtime]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev/custom-runtime
          fetch-depth: 0
      - name: Apply M3.5.8 patch
        run: python scripts/m3_5_8_glow_patch.py
      - name: Commit M3.5.8
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm scripts/m3_5_8_glow_patch.py
          git add src/control/activity-indicator.ts
          git commit -m "fix: soften Windows Computer Use activity glow"
          git push origin HEAD:dev/custom-runtime
